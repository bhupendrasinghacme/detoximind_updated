import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'detoximind_updated',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
